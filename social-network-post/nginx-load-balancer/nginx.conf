access_log  /var/log/nginx/access.log;

upstream loadbalancer {
    server unialfa-nodejs_post-app_1:3000;
    server unialfa-nodejs_post-app_2:3000;
}
server {
    listen 80;  
    server_name nginx;

    location / {
        proxy_pass http://post-app:3000;      
    } 
}
