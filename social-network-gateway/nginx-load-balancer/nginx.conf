access_log  /var/log/nginx/access.log;

upstream loadbalancer {
    server social-network-gateway_gateway-app_1:3000;
    server social-network-gateway_gateway-app_2:3000;
}
server {
    listen 80;  
    server_name nginx;

    location / {
        proxy_pass http://gateway-app:3000;      
    } 
}
